# csy3056-ci-example
Simple CI example for Continuous Integration

Note: This repository uses a simple example application to demonstrate
Continuous Integration behaviour. In real systems (e.g. mobile banking),
the same CI process applies at much larger scale.

